<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/output.css">
    <title>Loggin4Bloggin</title>
</head>
<body>

<nav class="bg-white border-gray-200 dark:bg-gray-900">
    <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl p-4">
        <a href="/" class="flex items-center">
            <button type="button" class="text-white bg-gradient-to-r from-purple-500 to-pink-500 hover:bg-gradient-to-l focus:ring-4 focus:outline-none focus:ring-purple-200 dark:focus:ring-purple-800 font-medium rounded-lg text-sm px-5 py-2.5 text-center mr-2 mb-2">Loggin4Bloggin</button>
        </a>
        {{#if logged_in}}
        <div class="flex items-center">
            <a id="logout" class="text-sm text-blue-600 dark:text-blue-500 cursor-pointer hover:underline">Logout</a>
        </div>
        {{/if}}
    </div>
</nav>
<nav class="bg-gray-50 dark:bg-gray-700">
    <div class="max-w-screen-xl px-4 py-3 mx-auto">
        <div class="flex items-center">
            <ul class="flex flex-row font-medium mt-0 mr-6 space-x-8 text-sm">
                <li>
                    <a href="/" class="text-gray-900 dark:text-white hover:underline" aria-current="page">Home</a>
                </li>
                <li>
                    <a href="/dashboard" class="text-gray-900 dark:text-white hover:underline">Dashboard</a>
                </li>
                <li>
                    <a href="/login" class="text-gray-900 dark:text-white hover:underline">Login</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
<main class="container mx-auto my-3 space-y-4">
  {{{ body }}}
</main>
{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}
</body>

</html>